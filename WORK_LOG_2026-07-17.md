# 工作记录 — 2026-07-17

## 一、线上状态诊断

| 项目 | 状态 |
|------|------|
| 网站 | 🟢 正常运行 (Render + Cloudflare) |
| 多语言 (en/ru/ja/ko) | 🟢 全部可用 |
| 累计数据 | 40 个国家、34 笔已支付订单 |
| 7 日访问 | 267 次访问，峰值 80 次/天 (7/14) |
| 7 日收入 | $0（PayPal Sandbox 模式） |

## 二、代码提交 (5 commits)

```
fbf8234  feat: PayPal Business account full payment loop
1bd9117  feat: proactive result generation (no IPN wait)
da2d402  fix: PDT verification uses same sandbox/live URL
44964ff  add public /api/report?public=stats7d endpoint
2fdf1ab  sync: PayPal references + complete tracking
```

## 三、完成的改动

### 1. 支付闭环升级
- `src/lib/paypal.ts` — 默认邮箱改为 `22728717@qq.com`，PDT/IPN/Checkout 统一使用动态 URL
- 新增 `rm=2` 参数（PayPal 支付后 POST 返回 tx，触发 PDT 实时验证）
- `src/app/api/result/route.ts` — 主动生成结果：用户回到 success 页时服务器立即生成结果，不等 IPN
- `src/app/api/pdt/route.ts` — PDT 验证 + 立即生成结果（0 秒延迟）
- 三层保障：PDT 实时 → 主动生成 → IPN 兜底

### 2. 全站埋点补齐
- 新增 6 个 `trackClick` 事件：择日/手相/解梦/联系表单/邮件订阅/分享
- 全站共 28 个埋点事件覆盖用户完整行为漏斗
- 后端 `/api/track` 记录页面访问 + 点击事件 + geoip 定位

### 3. 文档同步
- `.env.example` — LS → PayPal + OpenRouter + Resend
- `PROJECT_SUMMARY.md` — 更新支付网关、Admin Token 脱敏、废弃标记
- `privacy/page.tsx` / `terms/page.tsx` — 支付说明 LS → PayPal（用户可见）
- `src/lib/lemon.ts` / `src/app/api/webhook/route.ts` — 标记 DEPRECATED

### 4. 公开报表端点
- `GET /api/report?public=stats7d` — 无需认证，返回 7 日汇总

## 四、待用户操作（PayPal 审查通过后）

### Render 环境变量
登录 https://dashboard.render.com → chinese-culture-app → Environment

| 变量 | 值 |
|------|-----|
| `PAYPAL_SANDBOX` | `false` |
| `PAYPAL_PDT_TOKEN` | (PayPal 商家工具中获取) |
| `PAYPAL_EMAIL` | `22728717@qq.com` |

### PayPal 商家工具设置
登录 https://www.paypal.com/businessmanage/account/websettings
- 选择："设置付款链接和按钮"
- 自动返回 (Auto Return) → 开启 → URL: `https://www.culture-of-china.com/success`
- 付款数据传输 (PDT) → 开启 → 复制 Identity Token
- 买家消息标题：`Thank you for supporting Chinese Culture Studio!`
- 买家留言：`Your reading will appear on culture-of-china.com right after payment. If you are not redirected automatically, click "Return to Chinese Culture Studio" to view your result.`

## 五、支付闭环架构（最终形态）

```
用户点击支付
  │
  ├──→ PayPal 付款页面（企业账户）
  │      └─ 显示买家留言、品牌信息
  │
  ├──→ [第1层] 跳回 /success?tx=XXXX
  │      └─ PDT 实时验证 (rm=2) → 0 秒出结果
  │
  ├──→ [第2层] 如果 PDT 失败
  │      └─ 服务器主动生成 → 首次轮询 2 秒出结果
  │
  └──→ [第3层] PayPal IPN 后台确认
         └─ 最终安全网
```
