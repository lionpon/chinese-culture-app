# 工作记录 — 2026-07-20

## 一、Neon → Supabase 数据库迁移

### 背景
- Neon PostgreSQL 即将收费，决定迁到 Supabase 免费层
- 项目 ID：`vnktcrolpcyktduldpfm`

### 迁移步骤
1. Supabase 注册项目 `chinese-culture`
2. 更新 `prisma/schema.prisma`：添加 `directUrl` 支持
3. 更新 `.env` / `.env.local`：DATABASE_URL + DIRECT_URL
4. `prisma db push` 本地 → Supabase 5432 直连 ✅
5. 4 表 schema 同步成功：Purchase / Visit / DailyReport / Subscriber
6. Neon 数据导出：❌ 网络不通，数据量极小（0收入），接受数据丢失

### 连接策略
| 环境 | 连接方式 |
|------|----------|
| 本地开发 | Supabase Direct (5432) — pooler 6543 GFW阻断 |
| Render 生产 | Supabase Pooler (6543) — 美国服务器不受限 |

## 二、Render 部署排查

### 问题 1：prisma db push 卡死
- PgBouncer 事务模式与 `prisma db push` 不兼容
- 修复：`package.json` 构建命令移除 `prisma db push`
- Commit: `bdc3e17`

### 问题 2：线上 1 秒后白屏
- 根因：旧版 `AutoDailyReport` 无 try-catch，DB 异常时抛错
- Suspense 不捕获异常，RSC 流传输到浏览器 → "Server Components render error"
- 新代码 (`99ec20b`) 已修复，等新部署上线后解决

## 三、部署链路

```
GitHub master (bdc3e17)
  ← P0+P2+P3+P4+埋点+Supabase迁移+构建修复
        │
        └── Render 线上 ⏳ bdc3e17 部署中
```

## 四、PayPal
- ✅ 企业审批已通过
- ⏳ 待网站部署稳定后配置

## 五、Git 提交

| Commit | 内容 |
|--------|------|
| `bdc3e17` | fix: remove prisma db push from build |
| `499076c` | docs: update CLAUDE.md |
| `510ea75` | migrate: Neon → Supabase |
| `99ec20b` | fix: AutoDailyReport try-catch |
| `7d62e15` | fix: analytics tracking |
| `2d9341a` | feat: P4 guide tools |
| `763ee3f` | feat: P3 preview |
